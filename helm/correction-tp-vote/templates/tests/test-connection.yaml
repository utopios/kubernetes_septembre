apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "correction-tp-vote.fullname" . }}-test-connection"
  labels:
    {{- include "correction-tp-vote.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "correction-tp-vote.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
